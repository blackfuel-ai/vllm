# Minimal dependencies for CPU benchmarking (vllm bench).
# This is a curated subset of common.txt + cpu.txt,
# dropping multimodal, grammar, and serving-only packages.
#
# Pins track the in-tree common.txt / cpu.txt / build/cpu.txt so the
# CPU wheel built from this source imports cleanly when installed
# --no-deps into the final stage. Keep them in sync on every upstream
# sync that moves torch or a core runtime dependency.

# Core
numpy
tqdm
requests >= 2.26.0
pyyaml
typing_extensions >= 4.10
filelock >= 3.16.1
cloudpickle
pyzmq >= 25.0.0
msgspec
psutil
blake3
regex
cachetools
setproctitle
py-cpuinfo

# Model loading
transformers >= 5.5.3
tokenizers >= 0.21.1
sentencepiece
protobuf >= 5.29.6, !=6.30.*, !=6.31.*, !=6.32.*, !=6.33.0.*, !=6.33.1.*, !=6.33.2.*, !=6.33.3.*, !=6.33.4.*
pillow
compressed-tensors == 0.17.0
mistral_common >= 1.11.3

# Serving (needed by engine internals)
pydantic >= 2.12.0
fastapi[standard] >= 0.115.0, < 0.137
aiohttp >= 3.13.3
openai >= 2.0.0
prometheus_client >= 0.18.0
prometheus-fastapi-instrumentator >= 7.0.0

# Misc required by engine
partial-json-parser
einops
depyf==0.20.0
watchfiles
python-json-logger
pybase64
cbor2
ijson
openai-harmony >= 0.0.3
model-hosting-container-standards >= 0.1.14, < 1.0.0
tiktoken >= 0.6.0
lm-format-enforcer == 0.11.3
diskcache == 5.6.3
ninja
six>=1.16.0; python_version > '3.11'
setuptools==77.0.3; python_version > '3.11'

# CPU torch (no torchaudio/torchvision)
torch==2.11.0+cpu; platform_machine == "x86_64" or platform_machine == "s390x" or platform_machine == "aarch64"
torch==2.11.0; platform_system == "Darwin" or platform_machine == "ppc64le" or platform_machine == "riscv64"

# Intel OpenMP for x86_64
intel-openmp==2024.2.1; platform_machine == "x86_64"
